# Results

This folder contains the final outputs generated during the downstream genomic analysis.

The files include filtered variant summaries, gene frequency tables, functional enrichment results, and supporting datasets generated from Python and g:Profiler analyses.

Only processed results are included. Intermediate files and raw sequencing data are intentionally omitted.
